import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saidazimxon.valyuta',
  appName: 'Valyuta Kurslari',
  webDir: 'build'
};

export default config;
